# ── Slunder Studio — Dependencies ──────────────────────────
# Core runtime dependencies (install explicitly before first run)
PySide6>=6.6
numpy>=2.0,<2.5
sounddevice>=0.4
soundfile>=0.12
huggingface_hub>=0.20
pyqtgraph>=0.13
librosa>=0.10
scipy>=1.18
psutil>=5.9
tqdm>=4.0
model-signing==1.1.1  # OMS model signature verification

# ── Optional: AI Models ─────────────────────────────────────────────
# Do not install broad AI minimums into the application environment.
# Use tools/dependency_profiles.py with a hash-locked CPU/CUDA/MPS profile.
# Security floors: torch>=2.10.0 (CVE-2026-24747 / GHSA-63cw-57p8-fm3p)
# and transformers>=4.53.0. DirectML remains disabled until its supported torch
# version clears the torch floor.

# Lyrics Generation (GGUF)
# llama-cpp-python==0.3.34  # llama.cpp@e3546c794, past the b8146 GGUF fixes

# MIDI Studio
# pretty_midi>=0.2.10
# pyfluidsynth>=1.3        # + FluidSynth system lib + SoundFont

# Vocal Suite
# onnxruntime>=1.16         # DiffSinger
# onnxruntime-gpu>=1.16     # DiffSinger (GPU)
# librosa>=0.10             # Audio processing
# faiss-cpu>=1.7            # RVC feature index

# Stem Separation
# demucs>=4.0
# audio-separator[cpu]>=0.44.5  # maintained MDX/MDXC/Roformer adapter; install explicitly

# SFX Generation
# stable-audio-tools>=0.0.12

# Mastering / Audio Analysis
# librosa>=0.10

# ── Optional: Utilities ────────────────────────────────────────────
# pypinyin>=0.49           # Chinese phonemizer (DiffSinger)
# g2p-en>=2.1              # English phonemizer (DiffSinger)
# pyyaml>=6.0              # Config parsing
